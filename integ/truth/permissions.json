{
  "runs": {
    "python": {
      "command": [
        "./python/.venv/bin/python",
        "examples/python/permissions/permissions.py"
      ]
    },
    "typescript": {
      "command": [
        "pnpm",
        "-C",
        "examples/typescript",
        "exec",
        "tsx",
        "permissions/permissions.ts"
      ]
    }
  },
  "exit": 0,
  "lines": [
    "oncall     cat /repo/service.py                       [0] print('serving')",
    "                                                      the allow list installed cat",
    "oncall     sort /repo/service.py                      [127] sort: command not found",
    "                                                      unlisted: not a command, not a refusal",
    "oncall     ls /repo                                   [0] service.py",
    "                                                      the hide takes .env out of the listing",
    "oncall     cat /repo/.env                             [1] cat: /repo/.env: No such file or directory",
    "                                                      and answers absence, not refusal",
    "oncall     cat /runbook/local.env                     [0] PAGER=cat",
    "                                                      the same pattern, anchored to /repo, reaches no further",
    "oncall     ls /vault                                  [0] aws.token",
    "                                                      a deny leaves the name where it is",
    "oncall     cat /vault/aws.token                       [1] cat: /vault/aws.token: credentials are never read by hand",
    "                                                      and refuses the read: paths, depth 1",
    "oncall     rm /runbook/steps.md                       [126] rm: Permission denied",
    "                                                      ask at depth 1",
    "oncall     rm /runbook/frozen/rollback.md             [1] rm: /runbook/frozen/rollback.md: the rollback plan is frozen",
    "                                                      deny at depth 2 outranks it",
    "oncall     cp /vault/aws.token /runbook/steps.md      [1] cp: /vault/aws.token: credentials are never read by hand",
    "                                                      every operand is judged: the source's deny wins",
    "oncall     rm /repo/service.py                        [1] rm: cannot remove '/repo/service.py': Read-only file system",
    "                                                      the mount section narrowed the mode",
    "auditor    ls /vault                                  [2] ls: cannot access '/vault': No such file or directory",
    "                                                      hidden here, so the mount is not there at all",
    "auditor    cat /repo/.env                             [0] DSN=postgres://localhost/app",
    "                                                      the hide was the other role's, not the workspace's",
    "auditor    rm /runbook/steps.md                       [127] rm: command not found",
    "                                                      no rm in this allow list",
    "commander  sort /repo/service.py                      [0] print('serving')",
    "                                                      no allow list, so every tool is installed",
    "commander  wc -l /runbook/steps.md                    [0] 1 /runbook/steps.md",
    "                                                      including the ones no other role names",
    "commander  cat /vault/aws.token                       [1] cat: /vault/aws.token: credentials are never read by hand",
    "                                                      and it is still not unrestricted: the deny runs",
    "commander  rm /repo/service.py                        [0]",
    "                                                      no mount section, so /repo keeps the workspace's write mode"
  ]
}
